apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: bookish
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: hophopp/bookdev-frontend:latest
          ports:
            - containerPort: 5173
